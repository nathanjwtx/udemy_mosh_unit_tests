using System;

namespace mathtesting
{
    public class MathTests
    {
        public int Division(int x, int y)
        {
            if (x == 0 || y == 0)
            {
                return 0;
            }

            return x;
        }
    }
}
